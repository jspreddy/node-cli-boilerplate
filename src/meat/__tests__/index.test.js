import * as meat from '..';

describe('meat/index.js', () => {
  describe('chop', () => {
    it('should be a function with proper signature', () => {
      meat.chop();
    });
  });
  describe('mash', () => {
    it('should be a function with proper signature', () => {
      meat.mash();
    });
  });
});
